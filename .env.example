# Local LLM (Ollama). If unreachable, chat falls back to extractive answers.
RM_OLLAMA_URL=http://localhost:11434/api/generate
RM_MODEL_NAME=gemma4:e4b
RM_LLM_TIMEOUT_SEC=120
RM_MOCK_LLM=false

# Data roots (relative to project root by default)
RM_DATA_DIR=data
